<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kumar.save_plus.kumar_lazy.feature_03_login_register">

    <permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".dialog.SelectProfile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserActivities.UserLoginActivity" />
        <activity android:name=".UserActivities.Dashboard" />
        <activity android:name=".admin.AdminLoginActivity" />
        <activity android:name=".UserActivities.UserRegisterActivity" />
        <activity android:name=".admin.AdminDashboard" />
        <activity android:name=".dialog.DialogCapture"></activity>
    </application>

</manifest>